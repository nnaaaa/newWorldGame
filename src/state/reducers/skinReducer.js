import Skin from '../../assets/skins'
const init = Skin.e1

const skin = (state = init, action) => {
    // if (action.skin)
    //     return Skin[action.skin]
    return state
}

export default skin